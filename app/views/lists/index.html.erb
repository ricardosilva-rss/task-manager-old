<h1>My lists</h1>

<% @lists.each do |list| %>
  <div class = "list-display">
    <div class = "list-card">
      <p><%= link_to list.title, list_path(list) %></p>
      <p><%= list.description %></p>
    </div>
  </div>
<% end %>
